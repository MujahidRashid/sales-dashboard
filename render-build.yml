services:
  - type: web
    name: sales-dashboard
    env: node
    region: singapore
    plan: free
    buildCommand: npm install && cd client && npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
