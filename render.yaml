services:
  - type: web
    name: sales-dashboard
    env: node
    region: ohio
    plan: free
    buildCommand: cd client && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
